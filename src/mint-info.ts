export interface MintInfo {
    userAddress: string;
    tokenId: string;
    monsterId: string;
    name: string;
}

export interface TransferInfo {
    receiverAddress: string;
    tokenId: string;
}